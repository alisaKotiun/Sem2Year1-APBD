<EditForm Model="Movie" OnValidSubmit="OnValidSubmit">
    <DataAnnotationsValidator/>
    <div class="form-group">
        <label style="font-weight:bold">Title:</label>
        <div>
            <InputText class="form-control" @bind-Value="@Movie.Title" />
            <ValidationMessage For="@(() => Movie.Title)" />
        </div>

        <label style="font-weight:bold">Summary:</label>
        <div>
            <InputText class="form-control" @bind-Value="@Movie.Summary" />
            <ValidationMessage For="@(() => Movie.Summary)" />
        </div>

        <label style="font-weight:bold">Shown in theaters:</label>
        <div>
            <InputCheckbox class="form-control" @bind-Value="@Movie.InTheaters" />
            <ValidationMessage For="@(() => Movie.InTheaters)" />
        </div>

        <label style="font-weight:bold">Trailer:</label>
        <div>
            <InputText class="form-control" @bind-Value="@Movie.Trailer" />
            <ValidationMessage For="@(() => Movie.Trailer)" />
        </div>

        <label style="font-weight:bold">Release date:</label>
        <div>
            <InputDate class="form-control" @bind-Value="@Movie.ReleaseDate" />
            <ValidationMessage For="@(() => Movie.ReleaseDate)" />
        </div>

        <label style="font-weight:bold">Poster:</label>
        <div>
            <InputText class="form-control" @bind-Value="@Movie.Poster" />
            <ValidationMessage For="@(() => Movie.Poster)" />
        </div>
    </div>

    <button class="btn btn-success" type="submit">Save Changes</button>
</EditForm>

@code {
    [Parameter] public Movie Movie { get; set; }
    [Parameter] public EventCallback OnValidSubmit { get; set; }
}
